services:
  telegram-bot:
    build: ./bot
    container_name: telegram-bot
    environment:
      - TELEGRAM_BOT_TOKEN=${{ secrets.BOT_TOKEN }}
    # volumes:
    #   - .:/app 
    restart: unless-stopped
    ports: 
      - 7070:7070