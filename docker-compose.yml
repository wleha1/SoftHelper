services:
  telegram-bot:
    build: ./bot
    container_name: telegram-bot
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
    # volumes:
    #   - .:/app 
    restart: unless-stopped
    ports: 
      - 7070:7070